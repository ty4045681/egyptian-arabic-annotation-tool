Use case: precise-object-edit
这是刚生成的中文系统架构图，请生成它的正式汇报终稿。保持三列结构、模块、技术架构和深蓝/白/浅灰三色风格，专门修复背景、标签和错误连线。

最优先要求：画布必须是完全不透明的纯白色矩形背景，四周有白色留边。绝对不要透明背景，不要抠图，不要背景移除，不要黑色区域，不要透明像素、破洞、噪点、污渍或缺损。所有背景是实心白色或均匀浅灰，不使用渐变、阴影、纹理。画面仅三种设计颜色 #FFFFFF / #17324D / #E7ECF2。整个画面线条和文字要如排版软件导出般锐利干净。生成高分辨率 16:9 横版，尽可能 3840×2160。

请精准修复以下点：
1. 顶部主标题必须逐字为“埃及方言音频标注平台”，副标题“系统设计架构”，右上角“main 分支 · 5567209”。不可乱码。
2. 三列标题只保留“离线数据处理”、“在线标注与管理”、“媒体、交付与运维”。删掉所有“约 24%”、“约 50%”、“约 26%”及括号。比例本来只是构图指令，不能出现在图片上。
3. 删掉从左侧“本地音频目录”横跨“标注工作台”和“管理控制台”直至右侧音频服务的那条长水平线。这条线错误地连入了浏览器界面！右侧音频服务已有“读取本地音频目录”说明即可，不再画跨越浏览器的连线。左侧本地音频目录只画向下箭头至语音预处理。音频服务与接入层保留一条虚线，标注“音频授权与传输”。
4. 中央数据库逐字重写为下面五行：
“PostgreSQL”
“在线业务状态唯一真源”
“任务 · 基线 · 版本 · 片段 · 波形”
“任务持有关系 · 会话”
“幂等操作 · 审计”
给数据库区域足够空间，文字保持清晰，不要挤在一起。“幂等”二字必须正确。
5. 图底关键设计保障请准确保留：
“唯一约束防重复领取” | “版本号防陈旧覆盖” | “操作标识支持幂等重试” | “单事务发布与审计”
6. 页脚注释请准确重写，留足底边，不可裁切：
“入口按生产模板展示；腾讯云测试环境使用 SSH 隧道。”
“旧 JSON 仅作归档与兼容导出。”
7. 将中间连线上的“psycopg 连接池 · db.py”简化为“数据库连接池”；业务事务层可删去英文文件名以提升投影可读性。其他简短文件名可保留。
8. 所有文字用深蓝色无衬线中文黑体，字号清晰，背景不要透明。布局松紧适度，保证全图文字没有溢出边框、重叠、被连线穿过。

保留的架构事实：标注工作台和管理控制台共同进入 Cloudflare Tunnel → Nginx，再进入 Gunicorn + Flask，再进入业务事务层，再到 PostgreSQL；离线预处理为 Silero VAD + 可选 Qwen3.5-Omni，预处理入库直接写数据库；场景分类独立读写数据库；数据库连接数据交付、备份恢复。运行保障用虚线连接应用服务。不要引入新组件。
终稿必须完整不透明白底，文字笔画必须实心深蓝，不要透明描边。
