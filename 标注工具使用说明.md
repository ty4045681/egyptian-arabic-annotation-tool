# 埃及方言音频标注工具使用说明

本说明面向标注人员。请始终使用同一个用户名，系统会按用户名保存贡献、当前任务和完成历史。

## 1. 登录

打开管理员提供的网址，在 **Your Name** 输入姓名并点击 **Join**。

- 姓名最多 50 个字符，可包含字母、数字、空格、点、连字符和下划线。
- 一个名字同一时间只能有一个活跃登录。如果提示名字正在使用，请先在原设备 Logout，或等待会话超时。
- Logout 只退出登录，**不会释放未完成任务**。

## 2. 任务恢复与领取

登录后系统先检查您是否有未完成任务：

- 有任务：自动显示 **Resumed**，继续上次音频和最后一次成功保存的内容。
- 没有任务：显示 **Claim next task**，点击后领取下一条。
- 完成/跳过后不会自动抢下一条，您可以选择领取下一条或打开 **Completed data**。

未完成任务不会因为以下操作释放：

- 刷新页面；
- 关闭浏览器；
- Logout；
- 30 分钟会话超时；
- 服务重启；
- 长时间未操作。

只有四种方式会释放任务：完成、跳过、本人明确 Abandon、管理员明确回收。

## 3. 标注操作台

顶部显示：当前文件、用户、阿语转写字号调节（A− / A+，范围 14–32，保存在本机）、全局 Confirmed / Skipped / Pending / Total，以及 **Completed data** 入口。

任务工具栏包含：

| 控件 | 作用 |
|---|---|
| 音频播放器 | 连续播放完整音频 |
| Noisy / Not Egyptian / Poor quality | 选择整条音频的跳过原因 |
| Skip task | 按选择原因提交为 skipped |
| Previous completed | 只读查看本人最近完成记录 |
| Save draft | 立即保存所有未保存修改 |
| Mark done | 完成当前任务 |
| Abandon | 明确释放当前任务，其他人可领取；已成功保存的草稿仍保留 |
| Saved / Unsaved / Saving | 当前保存状态 |

## 4. 标准标注流程

1. 完整播放音频，了解上下文。
2. 从上到下处理每个 segment：
   - 点击 **▶** 播放本段，段尾自动暂停；中间暂停后再点会从当前位置继续，不必从头听。每段左侧有进度条，可拖动定位；
   - 在 Transcript 中填写核对后的阿拉伯语文本（默认字号已加大，可用顶部 **عربي A− / A+** 再调）；
   - 必要时调整 Start / End；
   - 无法用于训练的段点击 **Bad quality**。
3. 确认保存状态为 **Saved**。
4. 点击 **Mark done**。

Mark done 会再次由服务器检查：

- 所有未标记 Bad quality 的 segment 必须有文本；
- segment 时间不能重叠；
- segment 不能超过音频总时长；
- 页面必须仍持有该任务的有效 assignment。

最后一批未保存修改会和完成请求一起提交，不会出现“状态完成但最后输入没保存”的中间状态。

## 5. 跳过整条音频

使用场景：

| 原因 | 何时使用 |
|---|---|
| Noisy | 背景噪声过大，无法辨认 |
| Not Egyptian | 不是埃及方言或不是目标语言 |
| Poor quality | 录音失真、音量极低或质量不可用 |

先选择一个或多个原因，再点击 **Skip task**。跳过同样是正式提交；之后可在 Completed data 中查看并纠正。

## 6. Segment 表格

| 列 | 说明 |
|---|---|
| Play | 从本段起点播放，到段尾暂停 |
| # | segment ID |
| Start / End | `分:秒.毫秒`，相邻段不能重叠 |
| Duration | 自动按 Start / End 重算 |
| Transcript | 阿拉伯语文本，右到左输入；灰色 placeholder 是 ASR 参考，不算人工确认 |
| Quality | Bad quality 表示不参与训练，可不填文本 |

时间调整会被限制在上一段结束与下一段开始之间；服务端提交时还会再次校验。

## 7. 自动保存

- 修改文本、时间或 Bad quality 后，停止操作约 3 秒自动保存。
- 多个 segment 的修改会合并为一次请求。
- 同一任务只允许一个保存请求在途，避免互相覆盖。
- 网络失败时会使用同一操作编号安全重试。
- **Unsaved**：浏览器有尚未确认的修改。
- **Saving…**：正在保存。
- **Saved**：服务器已确认。
- **Save failed**：网络或服务器暂不可用；页面保留本地内容并重试。
- **Conflict**：服务器版本比当前页面更新；系统停止覆盖，请重新加载。

离开页面前尽量确认显示 Saved。浏览器会在尚有未保存内容时提示确认。

## 8. 历史查看（Previous completed）

点击 **Previous completed** 后进入只读历史：

- **Newer / Older** 在最近 50 条服务端历史中往返；
- 刷新和重新登录后历史仍存在；
- 当前未完成任务仍由您保留；
- 历史查看不会创建、释放或替换 assignment；
- 点击 **Return to current task** 回到当前标注。

历史视图不可直接编辑。需要纠正时进入 Completed data。

## 9. Completed data（本人已完成数据）

该页面只显示当前用户名提交的 annotated / skipped 数据：

- 汇总本人完成数、跳过数和累计音频时长；
- 按状态筛选；
- 搜索文件名或文件夹；
- 分页加载；
- View 查看音频、ASR、人工文本、时间和质量标记。

### Correct（纠正）

- 有未完成任务时 Correct 禁用：先返回标注页完成或明确 Abandon。
- 没有任务时可点击 Correct，系统复制当前已发布版本为新的纠正草稿。
- 纠正中途退出后，重新登录会强制恢复这条纠正任务。
- 纠正草稿自动保存时不会提前改变原已完成数据。
- 重新 Mark done / Skip task 后，新版本才正式发布。
- Abandon 纠正时，原已发布版本保持不变。

## 10. 累计时长和排行榜

登录页与标注页排行榜统一显示 `xx h xx min`，例如：

```text
12 h 07 min
```

排行榜只累计正式 completed（annotated）音频时长；skipped 显示数量但不计入完成时长。

## 11. 快捷键

| 快捷键 | 作用 |
|---|---|
| Enter（登录页） | 登录 |
| Ctrl/Cmd + S | 保存 draft |
| Ctrl/Cmd + Enter | Mark done |
| Tab / Shift+Tab | 按页面顺序切换输入框和按钮 |

## 12. 常见提示

| 提示 | 含义与处理 |
|---|---|
| No task available to claim | 全部已完成、暂时被他人占用，或尚未导入预处理任务；看页面具体说明 |
| Segments without text… | 填写列出的 segment，或标记 Bad quality |
| revision conflict | 页面已陈旧，重新加载；不要继续在旧页面编辑 |
| Lease token does not match | 当前页面不再拥有该 assignment，重新登录/加载 |
| Network connection failed | 网络中断，本地内容仍在页面；恢复后点击 Save draft |
| Audio could not play | 刷新后重试；持续失败联系管理员并提供文件名 |
| Correct 按钮不可用 | 您还有未完成任务，先返回处理 |
| Username is already in use | 同名 session 仍活跃，在原设备 Logout 或等待超时 |

## 13. 注意事项

1. 始终使用同一个用户名。
2. ASR 文字只是参考，必须听音频核对后输入人工文本。
3. 不要打开多个标签页同时编辑同一任务；系统会用 revision 阻止陈旧覆盖。
4. 不要把 Abandon 当作 Logout：Abandon 会把任务释放给其他标注员。
5. 中途离开无需 Abandon；直接 Logout 即可，任务会保留并在下次登录恢复。
6. 发生错误时记录文件名、操作步骤和屏幕提示，交给管理员排查。
